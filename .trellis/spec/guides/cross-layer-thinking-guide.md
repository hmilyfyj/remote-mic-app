# 跨层检查

从遥控器事件追踪到设备适配、会话控制、音频输出与界面状态，逐段标注拥有者和可观察完成条件。
修改配置字段时搜索导入、导出、旧版本默认值、设备独立配置及 UI 的所有使用点。修改异步链路时覆盖快速重复触发、取消、断连、过期回调和跨设备隔离。
涉及音频时依据 HardwareVoiceAudioContract 验证首字延迟、正常尾音排空与异常恢复；真实设备结果按 Testing 手册记录。
